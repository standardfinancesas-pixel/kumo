node_modules
.turbo
.next
dist
out
build
.env
.env.local
.env*.local
*.log
.DS_Store
coverage
.expo
*.tsbuildinfo
supabase/.branches
supabase/.temp

# Foto original del perro de la dueña: ya está copiada en apps/landing/public/img/socio-perro4.jpg
WhatsApp Image*.jpeg

.vercel

# Los gráficos para la ficha de Play: se generan con
# apps/mobile/scripts/generar-graficos-play.mjs y se suben a mano al Console.
# No los usa ningún build, así que no van al repo.
apps/mobile/scripts/salida-play

# La respuesta al equipo de revisión de Apple: se pega a mano en App Store
# Connect, igual que los gráficos de Play. No la usa ningún build.
apps/mobile/scripts/salida-apple
